import { OnlyAlphabeticDirective } from './only-alphabetic.directive';

describe('OnlyAlphabeticDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphabeticDirective();
    expect(directive).toBeTruthy();
  });
});
